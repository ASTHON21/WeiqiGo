
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 彻底放开权限，仅依赖应用层逻辑（FingerprintJS）进行棋手标识。
    // 这将解决在移除 Auth 后，由于浏览器残留的旧 Token 导致的 list/get 权限错误。
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
